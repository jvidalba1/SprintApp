<%=
active_admin_form_for(resource) do |f|
	f.inputs "Detalles del equipo" do
	  f.input :name, :label => "Nombre"
	  f.input :description, :label => "Descripción"
	  f.input :admin_users, :label => "Miembros", :as => :check_boxes
	end
	f.buttons
end
%>