<%=
active_admin_form_for(resource) do |f|
	f.inputs "Detalle de prioridades" do
	  f.input :name, :label => "Nombre"
	  f.input :weight, :label => "Peso", :as => :select, :collection => TicketPriority::WEIGHTS, :member_label => :titleize
	end
	f.buttons
end
%>